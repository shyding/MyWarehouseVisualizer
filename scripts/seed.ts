#!/usr/bin/env ts-node
import process from 'node:process';

const profile = process.argv[2];

console.log(`Seed generator for profile "${profile ?? 'unknown'}" 尚未实现。`);
console.log('请参考 README.factory-designer.md 的规划补全数据集生成逻辑。');
